# Plans

One file per implementation plan: `P###-<slug>.md`, created by the `/plan` workflow from
`.ai/templates/plan.md`. Plans describe bounded future work and its execution state;
general documentation lives in the other `docs/` files.
